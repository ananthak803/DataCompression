<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Compression Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: 'Inter', sans-serif;
      }
    </style>
  </head>

  <body class="bg-gradient-to-br from-gray-100 via-white to-gray-200 min-h-screen px-4 py-8">
    <div class="max-w-7xl mx-auto">
      <h1 class="text-4xl font-bold text-center text-gray-800 mb-10">
        📊 Compression Analytics Dashboard
      </h1>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
        <!-- Card -->
        <div class="bg-white rounded-2xl shadow-lg p-6 border border-gray-100 hover:shadow-xl transition">
          <h2 class="text-sm text-gray-500 uppercase tracking-wider mb-2">
            Total Entries
          </h2>
          <p class="text-3xl font-bold text-blue-600"><%= totalEntries %></p>
        </div>

        <div class="bg-white rounded-2xl shadow-lg p-6 border border-gray-100 hover:shadow-xl transition">
          <h2 class="text-sm text-gray-500 uppercase tracking-wider mb-2">
            Original Size
          </h2>
          <p class="text-3xl font-bold text-green-600"><%= totalOriginalSize %> B</p>
        </div>

        <div class="bg-white rounded-2xl shadow-lg p-6 border border-gray-100 hover:shadow-xl transition">
          <h2 class="text-sm text-gray-500 uppercase tracking-wider mb-2">
            Compressed Size
          </h2>
          <p class="text-3xl font-bold text-red-600"><%= totalCompressedSize %> B</p>
        </div>

        <div class="bg-white rounded-2xl shadow-lg p-6 border border-gray-100 hover:shadow-xl transition">
          <h2 class="text-sm text-gray-500 uppercase tracking-wider mb-2">
            Avg. Compression Ratio
          </h2>
          <p class="text-3xl font-bold text-purple-600"><%= averageCompressionRatio %></p>
        </div>

        <div class="bg-white rounded-2xl shadow-lg p-6 border border-gray-100 hover:shadow-xl transition">
          <h2 class="text-sm text-gray-500 uppercase tracking-wider mb-2">
            Compression Efficiency
          </h2>
          <p class="text-3xl font-bold text-indigo-600"><%= compressionEfficiency %> %</p>
        </div>
      </div>

      <div class="text-center text-gray-400 text-sm mt-12">
        &copy; 2025 • IoT Compression Dashboard
      </div>
    </div>
  </body>
</html>
